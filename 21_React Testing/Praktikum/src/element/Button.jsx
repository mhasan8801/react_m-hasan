

const Button = (text) => {
    return (
        <button
            type="submit"
            id="submit"
            className="btn btn-primary"
        >
            {text}
        </button>
    )
}

export default Button;